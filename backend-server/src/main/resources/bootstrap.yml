spring:
  application:
    name: backend
  banner:
    location: banner.txt
  cloud:
    nacos:
      config:
        server-addr: 120.79.160.65:8848
        name: backend
        shared-dataids: nacos.yml,db.user.yml,oss.yml,jwt.yml #如是eureka注册，将nacos换成对应的eureka即可
        file-extension: yml
        namespace: 0abd1eef-7bdb-433d-967f-3c499a61998f
        refreshable-dataids: nacos.yml,db.user.yml,oss.yml,jwt.yml #如是eureka注册，将nacos换成对应的eureka即可
  profiles:
    active: test
  main:
    allow-bean-definition-overriding: true
server:
  port: 8883