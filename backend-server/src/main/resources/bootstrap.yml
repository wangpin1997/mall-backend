spring:
  application:
    name: backend
  banner:
    location: banner.txt
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        name: backend
        shared-dataids: nacos.yml,db.user.yml #如是eureka注册，将nacos换成对应的eureka即可
        file-extension: yml
        namespace: 75361628-fff1-43cf-bd04-4b887b16de02
        refreshable-dataids: nacos.yml,db.user.yml #如是eureka注册，将nacos换成对应的eureka即可
  profiles:
    active: test
server:
  port: 8883